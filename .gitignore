node_modules/
dist/
.vite/
.DS_Store
*.log
dev_*.txt
.env
.env.local

# Junction to ../3D models — created locally by setup script (see scripts/setup-models.cjs).
# The junction itself is not committed; the source assets live in /3D models/.
public/models/
